#!/bin/bash
DIR="$(cd "$(dirname "$0")/../../.." && pwd)"
cd "$DIR"

export PATH="/usr/local/bin:/opt/homebrew/bin:$PATH"

# Build silently
npm run build > /tmp/mytube2-build.log 2>&1

# Launch server
nohup node server.js > /tmp/mytube2.log 2>&1 &
